# Phase 3 progress: P3-W05 started, narrow test migration pending

Owner instruction `P3-W05 启动` authorizes W05 from accepted W04 merge `a175d6d4b4153ddc9147301f9db8d247d12e4852`. The preceding owner instruction `批准` accepted the completed W04 package. PR #23 was normally merged and preserved reviewed head `f8fd0c91320e536e46c1e46f1dc5908f9f0f93cb` and tree `8e3549ff39fb2ffa04a1627106fc1ddca9b4a250`. The reviewed head's four-profile run `35602104824` passed with original-artifact and Git/history audits; the merge's supplementary service run `35643752188` also passed all four profiles. The latter is a service result, not a separately audited raw-artifact run.

W05's original nine paths cover the four common records, two scheduled analysis modules and three new unit-test files. Policy now identifies W05 and the two scheduled promotions. All forty-eight product modules and forty-seven existing test files remain byte-identical to accepted W04. Product implementation and new tests have not been delivered.

An isolated accepted-W04 clone exposed an inherited stage assumption before product implementation. `ModuleManifestTests.test_mutated_product_bytes_are_rejected` appends a pure `analyze` function to `analysis/origins.py` and expects rejection. That slot becomes live in W05, where the unchanged current guard correctly allows a pure function. With only the policy advanced, the same node changes from one pass at W04 to one failure at W05 (`AssertionError not raised`). These are targeted local probes, not a committed-head or complete-suite result.

Independent paired controls confirm that the unchanged W05 package is accepted, the same original mutation remains rejected in permanently inert `reporting/json_report.py`, and a forbidden effect in live `origins.py` remains rejected. The proposed `P3-W05-R01` preserves the old method identity, payload and rejection assertion, moves its inert-slot target, and adds explicit live-module controls. It requests exactly four additional paths: `tests/scaffold/test_module_manifest.py`, `tests/scaffold/test_ci_contract.py`, `tests/contract/test_phase3_transition.py`, and append-only `phase3/transition_ledger.md`. The raw plan, guard, catalogs, six existing migration pins and other units' immediate scopes stay fixed. This proposal has not been approved or applied.

The approved plan's sections 5 and 22 require a named approved amendment for this out-of-scope migration. W05 is paused at that boundary. Its twelve fields and forty-eight core obligations remain unimplemented/unverified. The sixteen previously verified M015 core rows remain intact, with all 228 integration and release rows pending. No W05 acceptance, W06 authorization or full-suite/CI pass is claimed. Any subsequent run on this blocked-record commit must retain its actual outcome.
